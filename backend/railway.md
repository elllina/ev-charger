# Railway doesn't need this file since Root Directory is set to backend
# If Railway is trying to cd into backend, remove the Custom Build Command in Railway UI

# Correct settings in Railway:
# Root Directory: backend
# Build Command: (empty - auto-detected from nixpacks.toml)
# Start Command: (empty - auto-detected from nixpacks.toml)
